# Base URL of the Mune Work API, including the /api/v1 prefix.
# Local development:
VITE_API_BASE_URL=http://localhost:3000/api/v1
#
# In production this MUST be set as a Netlify build environment variable
# (Site configuration → Environment variables), not here — a committed value
# is compiled into the bundle and shipped to every visitor.
